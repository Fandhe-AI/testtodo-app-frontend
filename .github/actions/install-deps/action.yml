name: 'Install dependencies'
description: 'Install pnpm dependencies with cache restoration'

runs:
  using: 'composite'
  steps:
    - name: Install dependencies
      run: pnpm install --frozen-lockfile
      shell: bash
